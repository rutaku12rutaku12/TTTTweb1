1. 인텔리제이 실행
- 상단메뉴 -> 파일 -> 프로젝트 닫기
2. 새 프로젝트
	JAVA프로젝트 선택
	1. 이름 : 아무거나
	2. 위치 : 바탕화면 (아무곳이나)
		-> git 저장소 생성 x  // 팀플때는 함
	3. 시스템빌드 : Gradle
		-> Gradle : 외부라이브러리 설치/세팅 소프트웨어
	4. JDK : 17
	5. Gradle : Groovy
	[생성]

3. Gradle 프로젝트 폴더구조
    .idea 폴더              : 인텔리제이 세팅파일
	.src 폴더               : (개발자가 작성한) 코드 파일
	-> main 폴더
	-> -> java 폴더         : 백엔드 위주 파일
	-> -> resources 폴더    : 프론트엔드 , 부가파일 등등
	build.gradle 파일       : 프로젝트내 라이브러리(남이만든코드들) 세팅 파일

4. 그레이들 환경에서 Spring 설치

	0. 접속 : https://start.spring.io/
	-- 왼쪽 화면 --
	1. project : Grade - Groovy
	2. Language : java
	3. Spring Boot : 3.5.4 버젼 (강의 기준)
	4. Project Metadata : 프로젝트 부가정보(기본값/생략)
	5. Packaging : jar
	6. JAVA : 17
	-- 오른쪽 화면 =-
	7. Dependencies (프로젝트에 설치한 라이브러리)
	        1). [ADD Dependencies]
	        2-1). mysql 검색후 -> MySQL Drive SQL 선택
	        2-2). Spring 검색후 -> Spring Web 선택
    -- 하단 화면에 --
    8. [ EXPLORE ]클릭
        1). build.gradle 코드 복사후 내 프로젝트내 build.gradle 파일에 붙여넣기
        2). 오른쪽 상단에 코끼리 (아이콘) 새로고침 클릭 후

5. 웹 환경 설정
    1. AppStart 클래스 위에 @SpringBootApplication
    2. AppStart 클래스 내 main함수 안에서
         - SpringApplication.run( 현재클래스명.class );
         예] SpringApplication.run( AppStart.class );
   확인 : AppStart 클래스 실행후 브라우저에서 http://localhost:8080/
        " Whitelabel Error Page" : 문구가 나오면 정상
        " 사이트에 연결할 수 없음" : 스프링 서버가 꺼져 있는 상태
    3. Dao 코드 유지 하고 Controller 클래스
    3-1 Controller 클래스명 위에 @RestController
    3-1 각 기능 메소드 위에
        - 저장 메소드(boardWrite) 위에 @PostMapping
        - 조회 메소드(boardWrite) 위에 @GetMapping

